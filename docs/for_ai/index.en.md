<p align="right"><b>English</b> · <a href="index.md">Русский</a></p>

# AI-agent map

Read this before opening source — it's the shortest path to the right file.

## Start here

1. **`src/pylzt/_MODULE_AUTO.md`** — top-level package map (what each subpackage owns).
2. Per-package `_MODULE_AUTO.md` — public surface for that package only, generated by
   `docskill`, kept current after every touch:
   - `src/pylzt/methods/_MODULE_AUTO.md` — `BaseMethod[T]`, the method-as-class contract.
   - `src/pylzt/models/_MODULE_AUTO.md`, `models/{market,forum,antipublic}/_MODULE_AUTO.md`
     — response DTOs, `LolzObject`/`BaseResponse` base classes.
   - `src/pylzt/facades/_MODULE_AUTO.md` — the generated `client.market`/`client.forum`/
     `client.antipublic` namespaces and their sync counterparts.
   - `src/pylzt/transport/_MODULE_AUTO.md` — the wire seam (`Request`/`Response`,
     `BaseTransport`, `HttpxSession`).
   - `src/pylzt/token_pool/_MODULE_AUTO.md`, `proxy_pool/_MODULE_AUTO.md` — rate-limit
     leasing and egress-proxy rotation.
   - `src/pylzt/storage/_MODULE_AUTO.md`, `export/_MODULE_AUTO.md`, `sync/_MODULE_AUTO.md`,
     `lib/_MODULE_AUTO.md`, `enums/_MODULE_AUTO.md` — persistence, cursor export, the sync
     engine, shared primitives (batch/retry/concurrency), generated `StrEnum`s.
   - `dev/codegen/_MODULE_AUTO.md` — the OpenAPI-spec-to-library generator.
3. Open real source only when a doc is missing, stale, or you need control flow the doc
   doesn't carry.

## Deeper references

- **[`../integration-guide.md`](../integration-guide.en.md)** — DI, config, fakes for tests,
  `reconfigure()` for live token rotation, the full typed-error table.
- **[`../codegen-runbook.md`](../codegen-runbook.en.md)** — live-verified vs. still-known-broken
  spec/reality mismatches, the hand-patch mechanism, and why (`_guard_no_clobber`).

## Non-obvious invariants worth knowing before editing

- **Method-as-class, not hand-written functions.** Every endpoint is a frozen
  `BaseMethod[T]` Pydantic model; `Client.execute(method)` is the one execution path
  every facade delegates to. Don't add a second request-building path.
- **Generated code is flat in the library, not a separate package.** Generated and
  hand-written modules coexist in the same directories — generated ones carry a
  `Generated by forge — DO NOT EDIT` header and an `{api}_`/`{api}`-prefixed name;
  hand-written ones are unprefixed. `install()` refuses to overwrite a hand-written
  module (`_guard_no_clobber`).
- **Sync isn't a second implementation.** `SyncClient`/`sync/runner.py` runs the same
  async engine on a background event-loop thread — there is exactly one rate-limit/retry
  implementation, not one per sync/async variant.
- **`GitHub Actions` is not the CI gate right now.** `.githooks/pre-push` (ruff check,
  ruff format --check, mypy, pytest) is — see the README's Development section.
